# MCPServer.AgentRouter.Infrastructure

Infrastructure adapters for the AgentRouter bounded context belong here.

Examples for later slices:

- MCP client/session adapter
- SSH tool adapter that calls the existing SSH MCP tools instead of embedding SSH directly
- filesystem trace store
- approval/consent persistence adapter
- model profile adapter

This project should not contain domain policy decisions or application orchestration.
